'use strict';

describe('writeFileProm', () => {
  beforeEach(() => {
    jest.spyOn('hub', 'emit');
  });
  afterEach(() => {
    jest.restoreAllMocks();
  });

  it('writes to a good file and emits saved', async (
    
  ))
})